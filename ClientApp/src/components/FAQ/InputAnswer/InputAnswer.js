import React from 'react';
import { FormControl } from "react-bootstrap";

const inputAnswer = (props) => {
    return (
        <div>
            <FormControl onChange={() => this.change} as="textarea"/>
        </div>
    );
}

export default inputAnswer;